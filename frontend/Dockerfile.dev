# Development Dockerfile for Pantry Frontend
FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source files
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Development mode with hot-reload
CMD ["npm", "run", "dev", "--", "--host"]
